// These imports should be covered by the "global: true" in vite.config.js
// However, this does not work, yet
import { it, expect } from "vitest"

it("should work", () => {
  expect(true).toBe(true)
})
