spring:
  cloud:
    kubernetes:
      config:
        enabled: true
        fail-fast: true
        sources:
          - name: database-config
          - name: oauth2-config
          - labels:
              spring-config: "true"
      secrets:
        enabled: true
        fail-fast: true
        paths: /etc/secrets
